import { CameraOptions, Camera } from '@ionic-native/camera/ngx';

export class OptionHelper {
    constructor(
    ) {
        
    }

    // static optionTakePictureBase64: CameraOptions = {
    //     quality: 100,
    //     destinationType: Camera.DestinationType.DATA_URL
    // }
}